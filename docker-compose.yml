services:
  backend:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8000:8000"
    volumes:
      - ${MEMORA_DATA_DIR:-~/.memora}:/root/.memora
      - .:/app
    env_file:
      - .env
    environment:
      - MEMORA_DATA_DIR=/root/.memora
